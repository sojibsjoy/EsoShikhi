<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kpsoftwaresolutions.esoshikhi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrivacyPolicyActivity"></activity>
        <activity android:name=".CompanyActivity" />
        <activity android:name=".BanglaNumberActivity" />
        <activity android:name=".EnglishNumberActivity" />
        <activity android:name=".BenjonbornoActivity" />
        <activity android:name=".SorobornoActivity" />
        <activity android:name=".NumberActivity" />
        <activity android:name=".AlphabetActivity" />
        <activity android:name=".BornomalaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>